Screen capture
    import file.jpg
fsck
    sudo touch /forcefsck
info
    cat /proc/cpuinfo

-------
version
-------

suse:
    $ cat /etc/SuSE-release
ubuntu:
    $ lsb_release -a
all:
    $ uname -a
